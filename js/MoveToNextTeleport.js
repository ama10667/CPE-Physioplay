// scripts/move-to-next-teleport.ts

import { Component, Type } from '@wonderlandengine/api';
import { property } from '@wonderlandengine/api/decorators.js'; // Ensure this is not causing conflicts

export class MoveToNextTeleport extends Component {
    static TypeName = 'move-to-next-teleport';

    static Properties = {

    };


    start() {
    }

    update(dt) {
    }

    // All previously declared methods were removed to avoid unused code errors
}